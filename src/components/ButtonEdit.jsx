import React, {useState} from 'react';
import { propTypes } from 'react-bootstrap/esm/Image';

const ButtonEdit = (props) => {

    return (
        <div className={props.class}>Edit</div>
    )
}

export default ButtonEdit;